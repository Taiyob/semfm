import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const p = await prisma.property.findFirst({ where: { title: { contains: 'Apartment T2 for Sale in Braga' } } }); if(!p) { console.log('Not found'); return; } console.log('URL:', p.externalListingUrl); try { const res = await fetch(p.externalListingUrl); console.log('Status:', res.status); const text = await res.text(); console.log('Contains imoovel vendido?', text.toLowerCase().includes('imóvel vendido')); console.log('Contains vendido?', text.toLowerCase().includes('vendido')); } catch(e) { console.log('Fetch error', e); } } main().finally(() => prisma.$disconnect());
